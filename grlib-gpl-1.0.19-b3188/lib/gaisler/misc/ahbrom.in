   bool 'On-chip AHB ROM                   ' CONFIG_AHBROM_ENABLE
    if [ "$CONFIG_AHBROM_ENABLE" = "y" ]; then
      hex 'ROM start address (haddr[31:20]) ' CONFIG_AHBROM_START 000
      bool 'Pipelined ROM access              ' CONFIG_AHBROM_PIPE
    fi

