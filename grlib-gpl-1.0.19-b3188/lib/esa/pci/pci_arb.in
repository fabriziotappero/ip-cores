  bool 'PCI arbiter  ' CONFIG_PCI_ARBITER
  if [ "$CONFIG_PCI_ARBITER" = "y" ]; then
    bool 'PCI arbiter APB interface ' CONFIG_PCI_ARBITER_APB
    int 'Number of PCI REQ/GNT pairs' CONFIG_PCI_ARBITER_NREQ 4
  fi

