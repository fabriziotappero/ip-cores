lightshow
---------

This example requires the Debug Board. It implements a light show using
the LED's. Effects are controlled by the switches.

Because FPGA Board pins C3 to D5 are connected to EZ-USB FX2 (rather than
to FPGA directely)  they have to be routed through the FX2.