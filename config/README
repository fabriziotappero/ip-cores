rename dot.config to ".config" and copy it inside your linux directory 
then you may review settings with "make nconfig" or launch kernel compile 
with "make"

This is to reproduce the kernel settings just like the kernel that comes 
inside the mcs file, incas eyou want to change/add something.
