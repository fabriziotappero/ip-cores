Scripts to configure tests:

iir (gen_coeffs.m): This script designs an example low-pass IIR filter and generates the header file for test (IIR6).
fir (gen_coeffs.m): This script designs an example low-pass FIR filter and generates the header file for test (FIR50).
fft (main_script.m): This script generates the VHDL description of ROMs for the FFT-core (based on the Adam Robert Miller work). 
